```text

void printValues(array){
  an array variable will be passed into the function
  go through the array with for loop
    print each element of the array
  print newline at the end 
  return void
}

swap(value1, value2){
  two int pointer values will be passed into the funtion
  create a temp variable for temporary values
  store value1 into temp
  copy value2 into value1
  copy temp into value2
}

sort(array){
  an array will be passed into the function
  start with the first pair of 2 elements within the array
  compare the two elements with if statement
  if left element > right element, swap them
  move to the next pair
  continue until you reach the end
  repeat until array is sorted
}

int main(){
  create an array of int values
  print those value as "Before:"
  test swap funtion by passing in two variables
  
  
 
 
```
